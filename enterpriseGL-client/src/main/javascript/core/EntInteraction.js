/** 
 * To change this template, choose Tools | Templates
 * and open the template in the editor.
 */
function EntInteraction(renderer){
    renderer.domElement.addEventListener( 'mousemove', EntInteraction.onMouseMove, false );
    renderer.domElement.addEventListener( 'mousedown', EntInteraction.onMouseDown, false );
    renderer.domElement.addEventListener( 'mouseup', EntInteraction.onMouseUp, false );
    
}

EntInteraction.onMouseMove = function(event){
    
};

EntInteraction.onMouseDown = function(event){
    
};

EntInteraction.onMouseUp = function(event){
    
};

